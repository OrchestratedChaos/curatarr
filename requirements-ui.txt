# Additional dependencies for the local web UI (dashboard,
# run-with-live-log, results, config screens) - see web/.
#
# Installed *in addition to* requirements.txt, only by run-ui.sh /
# run-ui.ps1 (and dev/test setups, and the PyInstaller binary build -
# see build-requirements.txt and curatarr.spec). Plain CLI/cron use via
# run.sh never needs these, so they're kept out of requirements.txt to
# keep that install lean.
#
# Pinned to exact versions, same convention as requirements.txt.
# run-ui.sh/run-ui.ps1 install from requirements-ui.lock (this file's
# direct + transitive deps, fully SHA256-hashed) when present - see the
# comment at the top of that file for the regenerate command.

# Local web UI framework
flask==3.1.3

# Round-trip YAML for the web UI config screens (preserves comments/
# structure in config/*.yml on save) - see web/config_io.py
ruamel.yaml==0.18.6
